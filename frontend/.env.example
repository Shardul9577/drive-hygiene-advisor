# Local only — Vite talks straight to Express.
VITE_API_URL=http://localhost:4000

# On Vercel: DELETE VITE_API_URL (or leave it empty).
# The frontend then calls /api/* on the same origin; vercel.json proxies those
# to Render so the session cookie stays on *.vercel.app.
